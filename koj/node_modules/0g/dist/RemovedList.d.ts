import { Entity } from './Entity';
/**
 * Manages "removed" Entities, stuck in limbo between being
 * 'deleted' (from user perspective) and formally returned to
 * pools.
 */
export declare class RemovedList {
    private _list;
    private _lookup;
    add: (entity: Entity) => void;
    flush: (callback: (entity: Entity) => void) => void;
    get: (entityId: number) => Entity<import("./Component").ComponentType<any>, any>;
}
